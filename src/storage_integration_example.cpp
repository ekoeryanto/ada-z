// storage_integration_example.cpp (moved to docs/)
// Demo moved to docs/storage_integration_example.cpp. This source file is retained
// only as a non-compiled pointer to the demo. The demo is documentation-only and
// will not be compiled into the firmware.

#if 0
#include <Arduino.h>
#include "storage_helpers.h"
// Original example integration: call these from your main sketch's setup() and loop().
#endif
